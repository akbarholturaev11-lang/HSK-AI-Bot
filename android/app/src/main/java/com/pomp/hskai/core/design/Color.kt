package com.pomp.hskai.core.design

import androidx.compose.ui.graphics.Color

/**
 * HSK AI brand tokens, taken from the Course v3 palette so the Android client
 * looks like the same product as the Mini App and the desktop app.
 *
 * Material 3 is only the technical base here — the default Material blue is
 * never used.
 */
object PompColors {
    val Paper = Color(0xFFFDF9F0)
    val PaperRaised = Color(0xFFFFFDF8)
    val Ink = Color(0xFF211D17)
    val InkSecondary = Color(0xFF665D50)
    val InkDisabled = Color(0xFFA79C8C)

    val Cinnabar = Color(0xFFE04A40)
    val CinnabarDark = Color(0xFFB23530)
    val CinnabarSoft = Color(0xFFFBE6E3)

    val Jade = Color(0xFF2FA06A)
    val JadeSoft = Color(0xFFE2F3EA)

    val Gold = Color(0xFFE9A916)
    val GoldSoft = Color(0xFFFBF0D6)

    val Divider = Color(0xFFE9E0CE)
    val Locked = Color(0xFFD8CFBE)
}
