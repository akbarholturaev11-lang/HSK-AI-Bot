<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ backup/transfer rules. Credentials, cached course data and
    widget state are never included in cloud backup or device-to-device
    transfer: a restored install must link to Telegram again.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
